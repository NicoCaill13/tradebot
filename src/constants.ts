import {
    FILTER_MARKET_CAP_MAX,
    SIZING_ADV_PCT_CAP
  } from './settings.js';
  
  export const MICROCAP_LIMIT = FILTER_MARKET_CAP_MAX; // < $300M par défaut
  export const ADV_PCT_CAP    = SIZING_ADV_PCT_CAP;    // 15% par défaut